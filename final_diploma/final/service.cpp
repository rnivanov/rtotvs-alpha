//--->—À”∆≈¡Õ€≈ ‘”Õ ÷»»

#include "service.h"

string IntToString(int buf)
{
	std::ostringstream buffer;
	buffer << buf;
	return buffer.str();
}

string DoubleToString(double buf)
{
	std::ostringstream buffer;
	buffer << buf;
	return buffer.str();
}